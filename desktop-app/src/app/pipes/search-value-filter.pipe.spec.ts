import { SearchValueFilterPipe } from './search-value-filter.pipe';

describe('SearchValueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchValueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
